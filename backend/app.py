# will contain fastapi based python backend code
# all apis will be listed here

# apis
# career selector

# ai requirememts gather (optional - as data will be provided with carrer only) [skipping for now]

# ai forms filler

# chatbot

# all clients json
    #full workflow json - all sections json objects

# alert generation
    #takes workflow json and tell what is missing and how many days deadline are remaining
    # example
    #   {isUrgent && <li>Need-by date is approaching in {Math.max(needByDays, 0)} day(s).</li>}
            #     {hasMissing && <li>Missing items: {lead.missing_items?.join(", ")}</li>}
            #   </ul>      

# form assistant



# i think we just need -> chatbot, ai form filler - with chat and voice -> and other things are good in examples existing, alert generation can be the thing if required